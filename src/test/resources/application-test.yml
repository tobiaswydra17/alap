spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/alap_test
    username: alap_user
    password: alap_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  seed:
    enabled: false